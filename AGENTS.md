# 竹南冷凍倉儲：Codex 任務片段表

版本：2026-09-27 校訂。放在 `KL-0728/system` 根目錄，與 spec.md 4.2 搭配。

## 執行方式

使用者說「我是 B，現在做 B1」等指令時，讀本檔、最新 `spec.md`、`README.md` 和現有程式，實際完成指定片段的畫面／API／資料處理與必要測試；不只交計畫，也不自行做下一片。缺依賴要明說，不能用假成功、繞過登入或另一套共用服務代替。規格定義功能與資料，README 記錄實際啟動及介面；發現衝突先指出。
- 一律沿用 React／TypeScript／Vite、FastAPI、SQLite 及現有骨架；資料表 SQL 的任何新增或遷移須由 A 協調，既有資料不得直接重建。共用介面變更先交 A 協調。各模組頁面都要能在手機操作，提交時停用按鈕，結果來自實際資料庫。
- Codex 在本機 repo 內可協助準備指定任務的功能分支。先檢查分支及未提交修改；新任務從最新 `main` 建立如 `feature/b-b1-inbound`，只允許安全的 fast-forward 更新，不在 main 寫。已有同任務分支則續用。若切換會覆蓋修改、分支分岔或衝突，停下說明；不自動丟棄、stash、reset 或 Force Push。
- **功能分支不得追蹤 `origin/main`。** 建立新分支前先 `git fetch origin`，再用 `git switch --no-track -c feature/<組員>-<片段>-<名稱> origin/main`；`origin/main` 只是起點，不是 upstream。建立後立即執行 `git status --short --branch` 與 `git branch -vv`。若功能分支旁顯示 `[origin/main]`，必須停止工作並回報，先用 `git branch --unset-upstream` 修正；不得按 VS Code「同步變更」或 Push。
- Commit／Push 前必須再次執行 `git branch --show-current` 與 `git branch -vv`。目前分支不可是 `main`，upstream 只能不存在或是同名的 `origin/feature/...`；任何功能分支若追蹤 `origin/main`，一律視為阻擋條件。首次上傳使用明確分支名，例如 `git push -u origin feature/b-b1-inbound`，不能把目的地寫成 `main`；確認 GitHub 出現該功能分支後才回報 A。Codex 即使被要求 Push，也要先做這項檢查。
- 下一片依賴未合併的上一片時，留在原功能分支續做並提醒使用者回報 A；原分支已合併則從最新 main 開新分支。不要把「Pull 功能分支」當成已整合 main，整合需要時由 A 協助。
- 片段完成後回報分支名稱、改動、實際通過／未完成的檢查及簡短手動驗收步驟。由本人操作後 Commit、Push 並告知 A；**B／C／D 不用開 PR，A 統一建立 PR、拉取實測、修正與合併**。除非使用者另外要求，Codex 不自行 Commit／Push；合併由 A 決定。
- 不刪／重置既有資料庫，不提交 DB、DB 備份、真實密碼、.env、.venv、node_modules 或 dist；依賴清單及鎖定檔要提交。安裝或介面改變時更新 README。只完成本次必做，不自行啟動加做。

## 開發順序

**A0 → A1 → A3 先合併，再並行 A2、B1→B2、C1→C2、D1→D2→D3，最後 A4。** 保留原片段編號，只調整 A 的次序讓共用介面先交付。A1/A3 未好前，其他人可讀規格、整理案例，不能宣稱正式串接完成；A3 的情境庫存讓 C、D 不必等 B1 才開發。保留交件前最後兩天整合和彩排。

## A（組長：基礎與整合）

- **A0｜確認骨架**：確認骨架可啟動、基礎 PR 已合併且 main 有最新版 spec.md、AGENTS.md；不重做骨架。檢查 .gitignore 及已追蹤清單，忽略規則不會自動移除已追蹤檔。
- **A1｜登入與基本資料種子**：登入頁、登入／登出／目前使用者、ADMIN／WORKER 的後端權限依賴及共用前端 API 呼叫；seed 兩種示範帳號（雜湊密碼）、A/B 庫、A-01/A-02/B-02/B-03/B-04 等儲位與品項，**不含甘藍菜**。提供兩角色可讀的品項／儲位清單 API，A2 稍後補編輯畫面。README 記錄介面、示範帳號及 `python -m backend.seed`。驗收：登入、登出有效，未登入受保護 API 被拒，重跑 seed 不重複也不覆寫資料。
- **A3｜共用庫存與開發資料**：建立 `backend/services/stock_service.py` 的交易、餘量／待審檢查與異動寫入介面；提供最小批次／位置／餘量選單 API 及共用前端型別，供 B/C/D 呼叫。seed 補紅蘿蔔 B-03 共5籠、青花菜 B-04 共3籠及入庫紀錄；以固定識別防止重複插入，保留已操作餘量。README 寫清楚函式及 API 用法、交易由誰開啟／提交，避免巢狀交易。驗收：失敗整筆回滾、餘量非負、同一待審批次／位置不能移出或移入；兩筆種子完整可讀。
- **A2｜品項、儲位與導覽**：管理者新增／編輯品項、單位、最低量／目標量及儲位，停用及歷史保留依 spec；沿用 A1 清單 API。共用導覽與倉管操作入口可在手機使用。驗收：新增刷新仍在、倉管不能修改、已有交易不能改品項單位；D3 的統計元件先預留位置。
- **A4｜整合展示**：各模組合併後補齊路由及首頁接合、修跨模組問題，整理 README 安裝／seed／展示／備份還原，實走 spec 第5節。需重演時先停服務保留原 DB，再初始化新的展示 DB 並 seed；不可把 seed 說成重置。驗收：手機與電腦連同一後端，入庫、出庫、移位、審核與報表一致。

## B（入庫與查詢）

- **B1｜單筆入庫**：表單＋API；選品項、位置、正整數數量及日期，由後端產生批次碼，同一交易建立批次、餘量與 RECEIPT 異動。使用 A1/A3 介面。驗收：入庫10籠後資料可讀且刷新不消失，0／負數／小數／停用儲位被拒且不寫入。
- **B2｜庫存查詢與歷史**：按品項、批次、儲位搜尋；顯示各處及合計、入庫日、庫齡、操作者及異動歷史。完整查詢 API 由 B 負責，不改 C/D 已依賴的 A3 選單契約。驗收：同批多處合計正確、空結果正常、手機可查；用測試資料驗證多處，C2 合併後再實測移位結果。

## C（出庫與移位）

- **C1｜出庫**：選單、表單＋API；用 A3 選單及 seed 庫存開發，同一交易扣量、寫 OUTBOUND。驗收：5籠出2籠剩3；超量／0／待審位置被拒；等待回應時連點不重複提交。操作結果未知時先查紀錄，不自動重送。
- **C2｜移位**：來源減、目標加並寫一筆 TRANSFER；位置不同、目標啟用、來源足夠，且來源及已存在目標的同批次／位置都不能待審。驗收：移位總量不變、超量／同位置／待審目標被拒、歸零餘量列保留。

## D（盤點、損耗與報表）

- **D1｜盤點與損耗申請**：用 A3 選單及 seed 庫存，倉管填非負盤點實數或正整數報廢量及原因，建立 PENDING 並保存原數，送件不改餘量。驗收：重複待審、無原因及超量報廢被拒；倉管能看自己的送件結果。
- **D2｜管理者核准與駁回**：管理者看到待審與詳情；交易內確認非申請本人、仍為 PENDING、餘量與原數一致，再一次完成審核及調整／異動；駁回必填原因且餘量不變。驗收：倉管／自己審自己／重複核准被拒；差額0只結案不寫零數量異動，待審來源與目標凍結有效。
- **D3｜決策報表與首頁統計**：後端計算管理者首頁統計、低庫存／補貨缺口、近30日出庫、庫齡、盤差／報廢分列；沒有批次的啟用品項按0計，不把不同單位混加。驗收：刷新取得新數字、倉管不能讀管理報表、手機可讀；先以 seed/測試資料驗證，與 B/C 的完整串接在 A4 彩排。
